# Production (Vercel): same-origin /api → Railway (vercel.json rewrite).
# Tránh CORS trình duyệt khi gọi API cross-origin.
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_ORIGIN=https://sedsp-api-production.up.railway.app
